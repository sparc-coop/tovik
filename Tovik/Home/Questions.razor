@page "/questions"

<section class="faq">
	<dl>
		@foreach (var question in FAQ.Take(Limit))
		{
			<div @onclick="@(() => Select(question.Key))" class="@(question.Key == SelectedQuestion ? "selected" : "")">
				<span>@( question.Key == SelectedQuestion ? "-" : "+")</span>
				<dt>
					@question.Key
				</dt>
				<dd>@((MarkupString)question.Value)</dd>
			</div>
		}
	</dl>

	<footer>
		@if (!IsExpanded)
		{
			<button class="secondary-btn" @onclick=Expand>View More</button>
		}
		Have additional questions?  <a href="mailto:support@sparc.coop">support@sparc.coop</a>
	</footer>
</section>

@inject ISparcBilling Billing
@inject ISparcAura Aura
@inject Microsoft.Extensions.Configuration.IConfiguration Config
@code {
	bool IsExpanded = true;
	void Expand() => IsExpanded = true;
	string? SelectedQuestion = "What is Tovik?";
	int Limit => IsExpanded ? FAQ.Count : 5;
	
	void Select(string question)
	{
		if (SelectedQuestion == question)
			SelectedQuestion = null;
		else
			SelectedQuestion = question;
	}

	Dictionary<string, string> FAQ = new()
	{
		{ "What is Tovik?", $"Tovik is a lightweight website plugin that automatically translates your content into each visitor's native language the moment they arrive." },
		{ "Why should I use Tovik?", "Your visitors shouldn't need to install anything just to read your site in their own language. Tovik removes that burden entirely: after a quick, one‑time install on your site, it automatically translates your content for every visitor, so your whole site appears in their native language with no extra steps for them and no ongoing work for you." },
		{ "How does Tovik work?", "Tovik detects your visitor's preferred language from their browser settings. If it differs from your site's language, Tovik immediately translates every part of the page into their native language. The translation appears almost instantly, and Tovik securely caches the result so future visitors using the same language and dialect get the translated version even faster." },
		{ "Will Tovik work with my site?", "If your site uses a standard CMS (like WordPress or Wix), or you can add custom HTML (like a Google Analytics tag), you're good to go. Tovik works with WordPress, Wix, Webflow, Weebly, SquareSpace, Shopify, and pretty much anything else." },
		{ "How do I install Tovik?", "You can install Tovik in just a couple of minutes: either download the plugin for your website's platform or paste a single line of HTML into your site." },
		{ "What does Tovik include?", "Tovik's Free edition covers up to 10 translated pages per domain. If you need more, the Personal edition expands that to 100 pages, and the Premium edition supports up to 1,000 pages. All editions include unlimited page views, unlimited languages and dialects, and unlimited bandwidth, with no monthly subscription required." },
		{ "There's really no subscription fee?", "Tovik is a one‑time purchase, with no subscriptions or recurring fees. We built it that way on purpose, so you can own your translation engine outright without ongoing costs or surprise charges." },
		{ "How many languages will Tovik translate my website to?", "Tovik speaks 200+ languages and 600+ dialects, giving your site natural coverage across most of the world's major languages and regions." },
		{ "Can I use Tovik on multiple domains?", "You can use Tovik on multiple domains when they're aliases of the domain you purchased. Any domain that isn't an alias will need its own separate license." },
		{ "What if I have more than my plan's pages of content?", "It's no problem! We track usage to ensure everyone is using Tovik fairly. We'll reach out if needed, but we'll never stop your service without getting in touch." },
		{ "Do you track user or visitor data?", "Nope! We only keep track of how many pages you've translated. Your visitors' data stays private in their browser, and we never use, track, or store any personal information." },
		{ "Can I get a refund if Tovik doesn't work for my site?", "Absolutely. Send us an email at <a href=\"mailto:support@sparc.coop\">support@sparc.coop</a> and we'll refund your purchase right away." },
		{ "How do I get support?", "You can always email us at <a href=\"mailto:support@sparc.coop\">support@sparc.coop</a>. We're here to help with anything you may need." }
	};
}